# Promptfoo + GroundGuard

Run a dependency-free example that emits promptfoo-style assertion JSON:

```bash
python examples/promptfoo_groundguard/run.py
```

Use this payload shape from a custom promptfoo assertion when you want
GroundGuard fact coverage to sit beside model-quality and red-team checks.

